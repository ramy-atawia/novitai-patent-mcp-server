# FastMCP Requirements
# Additional requirements for FastMCP server implementation

# FastMCP - High-level MCP framework
fastmcp>=2.0.0

# Core dependencies (already in requirements.txt, listed here for clarity)
fastapi>=0.108.0
uvicorn[standard]>=0.24.0
pydantic>=2.8.0
pydantic-settings>=2.6.1

# All other dependencies are already covered in requirements.txt
# To use FastMCP, install both:
# pip install -r requirements.txt
# pip install -r requirements-fastmcp.txt

